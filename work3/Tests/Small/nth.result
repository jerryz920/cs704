(define (main list) (cons (call nth-3 list) (call nth-7 list)))
(define (nth-3 list) (call nth-2 (cdr list)))
(define (nth-2 list) (call nth-1 (cdr list)))
(define (nth-1 list) (call nth-0 (cdr list)))
(define (nth-0 list) (car list))
(define (nth-7 list) (call nth-6 (cdr list)))
(define (nth-6 list) (call nth-5 (cdr list)))
(define (nth-5 list) (call nth-4 (cdr list)))
(define (nth-4 list) (call nth-3 (cdr list)))
