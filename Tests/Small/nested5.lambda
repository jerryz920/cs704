(letrec (length = (lambda (x) (if ((== x) nil)
                               then 0
			       else ((lambda (length) ((+ 1) length)) 22)
			      )
                  )
        ) in
  (length nil)
)




